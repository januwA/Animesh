- 使用TDD开发模式
- 优先使用shadcn组件库,能用组件库就不要编写自定义样式
- 不要使用大量的Emoji符号
- 错误处理规范：
  - 处理错误应该一直向上抛，不要在半路使用 console 打印拦截或吞掉错误。
  - 仅在最外层（如 UI 交互反馈、背景轮询）安全捕获“操作错误”并处理，Bug 类错误应任其自然崩溃暴露。
  - 重新包装并抛出新错误时，必须使用 `{ cause: err }` 选项保留原始错误链。
