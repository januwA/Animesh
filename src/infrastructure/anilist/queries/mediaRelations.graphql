query MediaRelations($id: Int!) {
  Media(id: $id) {
    relations {
      edges {
        relationType
        node {
          id
          title {
            romaji
            english
            native
            userPreferred
          }
          coverImage {
            large
            medium
          }
        }
      }
    }
  }
}
