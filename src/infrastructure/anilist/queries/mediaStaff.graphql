query ($id: Int!) {
  Media(id: $id, type: ANIME) {
    staff(sort: RELEVANCE) {
      edges {
        role
        node {
          id
          name { full }
          image { large }
        }
      }
    }
  }
}